

nomes = ''' CREATE TABLE  IF NOT EXISTS nomes (
                                    id int PRIMARY KEY,
                                    nome VARCHAR(10) NOT NULL,
                                    age INT NOT NULL )'''

marcas = ''' CREATE TABLE  IF NOT EXISTS marcas (
                                    id int PRIMARY KEY,
                                    nome VARCHAR(10) NOT NULL,
                                    age INT NOT NULL )'''
